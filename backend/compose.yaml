services:
  app:
    build: .
    env_file:
      - .env
    ports:
      - "8080:8080"

  mysql:
    image: mysql:8
    environment:
      - 'MYSQL_DATABASE=mydatabase'
      - 'MYSQL_PASSWORD=secret'
      - 'MYSQL_ROOT_PASSWORD=verysecret'
      - 'MYSQL_USER=myuser'
    ports:
      - '3306'
